services:
  redis:
    image: redis:latest
    restart: unless-stopped
    ports:
      - 6379:6379
    networks:
      - redis-net
    command: redis-server --requirepass {{ redis_password }}

networks:
  redis-net:
    driver: bridge
